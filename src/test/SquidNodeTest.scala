package test

import org.scalatest.FunSuite

/**
 * User: Alejandro Peláez
 * Date: 9/06/12
 * Time: 01:44 PM
 */

class SquidNodeTest extends FunSuite{
  test("Should receive message that belongs to itself")(pending)
  test("Should not receive message that do not belongs to itself")(pending)
  test("Should route messages correctly")(pending)
}
